using Test

# Test Riemann Solver against known solution
# Test finite volume solver
    # Exact solution and convergence rate (L1, L2 and L∞ norms)
    # smooth problems with grid refinement
    # conservative aspect of the scheme
    # shocks
    # 1D Riemann Problem (against Riemann solver)
    # 1D Riemann Problem in 2D (against Riemann solver)

# vtk saving and reading
